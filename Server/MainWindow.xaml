<Window x:Class="Server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Potapanje Podmornica - Server" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Parametri igre" Grid.Row="0" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Broj igrača:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox x:Name="txtBrojIgraca" Grid.Row="0" Grid.Column="1" Text="2" Margin="5,0,0,0"/>

                <TextBlock Text="Dimenzija table:" Grid.Row="0" Grid.Column="3" VerticalAlignment="Center"/>
                <TextBox x:Name="txtDimenzija" Grid.Row="0" Grid.Column="4" Text="5" Margin="5,0,0,0"/>

                <TextBlock Text="Broj promašaja:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox x:Name="txtPromasaji" Grid.Row="2" Grid.Column="1" Text="10" Margin="5,0,0,0"/>

                <TextBlock Text="UDP Port:" Grid.Row="2" Grid.Column="3" VerticalAlignment="Center"/>
                <TextBox x:Name="txtUdpPort" Grid.Row="2" Grid.Column="4" Text="5000" Margin="5,0,0,0"/>

                <Button x:Name="btnPokreni" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="5" 
                        Content="Pokreni Server" Click="BtnPokreni_Click" Height="30" Margin="0,0,0,0"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Status servera" Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <TextBlock x:Name="txtStatus" Text="Server nije pokrenut" FontWeight="Bold"/>
                <TextBlock x:Name="txtPrijavljeni" Text="Prijavljeni igrači: 0/0" Margin="0,5,0,0"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Igrači" Grid.Row="2" Margin="0,0,0,10">
            <ListBox x:Name="lstIgraci" Height="100"/>
        </GroupBox>

        <GroupBox Header="Log" Grid.Row="3">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="txtLog" IsReadOnly="True" TextWrapping="Wrap" 
                         VerticalAlignment="Stretch" AcceptsReturn="True"/>
            </ScrollViewer>
        </GroupBox>

        <Button x:Name="btnZaustavi" Grid.Row="4" Content="Zaustavi Server" 
                Click="BtnZaustavi_Click" Height="30" Margin="0,10,0,0" IsEnabled="False"/>
    </Grid>
</Window>